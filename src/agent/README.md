# Local Agent

This module runs on the retailer's premises and handles:
- Reading raw data from accounting / POS systems
- Processing and anonymizing sensitive business data
- Sending only aggregated insights to the cloud layer

> Raw data never leaves this module.
