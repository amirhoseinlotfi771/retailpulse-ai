# API Layer

Backend REST API routes:
- `/api/query` — conversational AI endpoint
- `/api/inventory` — inventory analytics
- `/api/sales` — sales trend data
- `/api/insights` — aggregated business insights
